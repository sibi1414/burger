<div class="header">
    <h1>Eat-Da-Burger</h1>
   </div>


<div class="row">
    <div class="section">
        <form class="create-update-form" action="/" method="POST">
            <div class="form-group">
                <label for="ca"> <font weight: lighter> Burger Name:</label>
                <input type="text" id="burgerInput" name="burger_name" placeholder="ex: Double Bacon Burger">
            <button class="btnColor btn btn-success" type="submit">Order Burger</button>
            </div>
        </form>
    </div>


    <div class="notDevoured section">
        <h2>Order</h2>
        <table class ="table">
            <thead>
            <tr>
                <th class="titleCenter">Burger</th>
                <th class="titleCenter">Devour</th>
            </tr>
            </thead>
            <tbody>
                <tr>
                {{#each burgers}}
                    {{#unless this.devoured}}
                            <td>
                                {{this.burger_name}}
                            </td>
                            <td>
                                <form action="/{{this.id}}  ?_method=PUT" method="POST">
                                    <input type="hidden" name="devoured" value="true">
                                    <button class="btnColor btn btn-success" type="submit">Devour it!</button>
                                </form>
                            </td>
                    {{/unless}}
                        </tr>
                {{/each}}
            </tbody>
        </table>
    </div>



    <div class="devoured section">
        <thread>
            <h2>Devoured</h2>
        </thread>
        <table class ="table">
                {{#each burgers}}
                    {{#if this.devoured}}
                        <tr>
                            {{this.burger_name}}
                            
                        </tr>
                        <br><br>
                    {{/if}}
                {{/each}}
            </table>
    </div>